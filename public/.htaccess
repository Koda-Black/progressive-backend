RewriteEngine On

# ALL requests go through PHP (including OPTIONS for CORS)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]

# NOTE: CORS headers are handled by PHP, not Apache
